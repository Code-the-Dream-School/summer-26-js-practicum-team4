Files within this folder will hold tests for two types of functions:

- Endpoints found within controller files **to ensure that an appropriate response is generated**.
- Mathematical logic found inside the image processing code file **to ensure that the file's basic logic is unbroken.**

Currently there are no test or controller files in the directory, but once a userController/patternController file is up, tester files and the appropriate imports will be present to follow as guidelines for future testing integration.
